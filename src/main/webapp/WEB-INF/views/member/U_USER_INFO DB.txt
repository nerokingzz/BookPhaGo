CREATE TABLE MEMBER
   (	
	BNO VARCHAR2(20 BYTE),
	USERID VARCHAR2(20 BYTE), 
	USERPASS VARCHAR2(20 BYTE), 
	USERNAME VARCHAR2(20 BYTE), 
	USEREMAIL VARCHAR2(20 BYTE), 
	USERAGE VARCHAR2(20 BYTE), 
	USERGENDER VARCHAR2(20 BYTE), 
	USERTASTE1 VARCHAR2(20 BYTE), 
	USERTASTE2 VARCHAR2(20 BYTE), 
	USERTASTE3 VARCHAR2(20 BYTE), 
	USERBADCNT VARCHAR2(20 BYTE), 
	REGDATE DATE DEFAULT sysdate
	
   );


	CREATE SEQUENCE MEMBER_SEQ
	START WITH 1
	INCREMENT BY 1;


ALTER SEQUENCE MEMBER_SEQ INCREMENT BY 1;
ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ

CREATE TABLE U_USER_INFO
   (   
   BNO NUMBER,
   USERID VARCHAR2(100), 
   USERPASS VARCHAR2(100), 
   USERNAME VARCHAR2(100), 
   USEREMAIL VARCHAR2(100), 
   USERAGE NUMBER, 
   USERGENDER VARCHAR2(100), 
   USERTASTE1 VARCHAR2(100), 
   USERTASTE2 VARCHAR2(100), 
   USERTASTE3 VARCHAR2(100), 
   BADCNT NUMBER, 
   BADSTS VARCHAR2(100),
   STOPSTART VARCHAR2(100),
   STOPEND VARCHAR2(100),
   BORROWCNT NUMBER,
   APPLYCNT NUMBER,
   RESERVECNT NUMBER,
   REGDATE DATE DEFAULT sysdate
   );

	CREATE SEQUENCE U_USER_INFO_SEQ
	START WITH 1
	INCREMENT BY 1;

ALTER SEQUENCE U_USER_INFO_SEQ INCREMENT BY 1;
ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ

BEGIN
FOR i IN 1..200 LOOP
INSERT INTO member(userid, userpass, username, useremail, userage, usergender, usertaste1, usertaste2, usertaste3, userbadcnt) 
VALUES(CONCAT('test', i), CONCAT('test', i), CONCAT('이름', i), CONCAT('이메일', i), CONCAT('나이', i), CONCAT('성별', i), CONCAT('취향1', i), CONCAT('취향2', i), CONCAT('취향3', i), CONCAT('cnt', i));
END LOOP;
END;

ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ

insert into member(userid, userpass, username)
values('admin', 'admin', '관리자');